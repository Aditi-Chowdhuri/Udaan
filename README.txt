You should have python 3 installed in your system

Run the following commands in the command line in the project directory:
pip install -r requirements.txt
python main.py

The URL for the hosted website will be displayed in the terminal window